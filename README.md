# Memoization Lab

This project demonstrates a simple memoization function in JavaScript.

## Features
- Caches function results
- Avoids repeated calculations

## How it works
The function stores previous results and returns them if the same arguments are used again.
